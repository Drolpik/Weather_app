import React from 'react';
import classes from './Logo.module.scss';

const logo = (props) => (
    <div className={classes.Container}>
        <h2>Weather App</h2>
    </div>
);

export default logo;